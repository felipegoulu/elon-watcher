# Gateway HTTP mode (get these from your OpenClaw config)
GATEWAY_URL=https://your-ngrok-url.ngrok.io
GATEWAY_TOKEN=your-gateway-token

# Or use CLI mode (if OpenClaw is installed on host)
# OPENCLAW_CMD=openclaw
